local Library = loadstring(game:HttpGet("ลิงก์สคริปต์ของคุณ"))()

local Window = Library:Window({
    Name = "Balright Hub",
    Size = Vector2.new(550, 400),
    SideBarSize = 160
})

local CombatPage = Window:Page({Name = "Combat", Icon = "rbxassetid://136879043989014"})
local CombatSubPage = CombatPage:SubPage({Name = "Main"})

local AimbotSection = CombatSubPage:Section({Name = "Aimbot", Side = 1})

AimbotSection:Toggle({
    Name = "Enabled",
    Callback = function(Value)
        print("Aimbot is:", Value)
    end
})
